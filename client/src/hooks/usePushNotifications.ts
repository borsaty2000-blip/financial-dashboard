import { useCallback, useEffect, useState } from 'react'
import { api } from '../lib/api'
function decodeKey(value: string) { const padding = '='.repeat((4 - value.length % 4) % 4); const base64 = (value + padding).replace(/-/g, '+').replace(/_/g, '/'); const raw = atob(base64); return Uint8Array.from([...raw].map((character) => character.charCodeAt(0))) }
export function usePushNotifications() { const [supported, setSupported] = useState(false); const [enabled, setEnabled] = useState(false); const [busy, setBusy] = useState(false); useEffect(() => { void (async () => { const available = 'serviceWorker' in navigator && 'PushManager' in window && 'Notification' in window; setSupported(available); if (!available) return; const registration = await navigator.serviceWorker.register('/sw.js'); setEnabled(Boolean(await registration.pushManager.getSubscription())) })() }, []); const enable = useCallback(async () => { setBusy(true); try { if (!supported) throw new Error('المتصفح لا يدعم الإشعارات'); const permission = await Notification.requestPermission(); if (permission !== 'granted') throw new Error('لم يتم منح إذن الإشعارات'); const status = await api<{ publicKey: string | null }>('/api/push/status'); if (!status.publicKey) throw new Error('إشعارات Push غير مهيأة على الخادم بعد'); const registration = await navigator.serviceWorker.ready; const subscription = await registration.pushManager.subscribe({ userVisibleOnly: true, applicationServerKey: decodeKey(status.publicKey) }); await api('/api/push/subscribe', { method: 'POST', body: JSON.stringify({ endpoint: subscription.endpoint, keys: subscription.toJSON().keys, deviceType: 'WEB' }) }); setEnabled(true) } finally { setBusy(false) } }, [supported]); return { supported, enabled, busy, enable } }
