# تدقيق واستيراد أسهم السوق السعودي — 15 سبتمبر 2026

تمت مراجعة الملف `أسهم_السوق_السعودية_مدققة.xlsx`. يحتوي على ورقة واحدة باسم `الأسهم السعودية` وعمودين فعليين فقط: `كود التداول` و`اسم الشركة بالعربي`. لم يوجد عمود ثالث مستقل، لكن الكود نفسه مختصر بصيغة TASI مثل `2222`.

تم إنشاء جدول `tasi_companies` بعملة `SAR`، ثم تطبيق migration واستيراد السجلات باستخدام upsert آمن قابل لإعادة التشغيل. النتيجة: **419 سجل TASI مستورداً، 0 محدث، 0 متروك**. مجموع كتالوجي السوقين أصبح **564 شركة**: 145 EGX و419 TASI.

| الحقل | النتيجة |
|---|---:|
| سجلات الملف | 419 |
| الأكواد الفريدة | 419 |
| الأسماء العربية الفريدة | 397 |
| تكرارات الأكواد | 0 |
| العملة | SAR |
| SHA-256 | `3adb7ed5d259f56a6c65182c96d7023eefd67385553abf039ac40ca9b06a8f8d` |

تم اختبار endpoints `GET /api/market/stats` و`GET /api/stock/:symbol` للأسهم 2222 و1120 و1180 و7010 و2010، إضافة إلى COMI وABUK. أعادت جميعها HTTP 200، والأسماء العربية، والسوق الصحيح، والعملة الصحيحة. يدعم جلب الشموع TASI تحويل الرمز إلى صيغة Yahoo `.SR` عند استخدام ذلك المصدر، مع الحفاظ على ترتيب المصادر الأخرى وfallback وعدم اختلاق الأسعار عند عدم توفر مزود.

نجحت فحوص Prisma وTypeScript، و26 اختبار خادم، وProduction build، و`git diff --check`.
