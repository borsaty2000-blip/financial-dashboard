# خريطة السوق الحرارية — 15 سبتمبر 2026

أضيفت صفحة `/heatmap` وخدمة `GET /api/v1/market/heatmap?market=EGX|TASI`. تعرض الصفحة الأسهم التي أعاد لها مزود السوق سعراً وتغيراً فعليين فقط، وتفتح كل بطاقة غرفة تحليل السهم مع رمز السوق والاسم. لا تُحوّل الأسهم غير المتاحة إلى صفر ولا تعرض أرقاماً افتراضية.

## التحقق

- `npm run typecheck`: ناجح.
- `npm run test:server`: 25 اختباراً ناجحاً.
- `npm test -- --run`: 94 اختباراً ناجحاً و6 متجاوزة عمداً.
- `npm run build`: ناجح.
- اختبار HTTP محلي لـ EGX: HTTP 200، 9 أسعار متاحة من أول 40 رمزاً محدوداً.
- اختبار HTTP محلي لـ TASI: HTTP 200، مع عدم توفر دليل محلي في بيئة الاختبار الحالية؛ أظهرت الواجهة حالة فارغة صريحة بدلاً من بيانات مصطنعة.

التغطية محدودة عمداً لحماية مزودي البيانات وعدم تنفيذ مئات الطلبات في طلب واحد. الدليل الكامل للأسهم المصرية والسعودية يبقى متاحاً في صفحات الأسواق، بينما تُظهر Heatmap اللقطة السعرية المتاحة فقط.
