# تدقيق واستيراد شركات البورصة المصرية — 15 سبتمبر 2026

تمت مراجعة الملف `أسهم_البورصة_المصرية_مدققة.xlsx` قبل الاستيراد. يحتوي المصنف على ورقة واحدة باسم `الأسهم` وبها 145 صفاً للبيانات وعمودان فقط: `كود التداول` و`اسم الشركة بالعربي`. لم يوجد عمود C مستقل للرمز المختصر، رغم وصف الطلب له؛ لذلك حُفظ الكود الأصلي كما ورد، واستُنتج الرمز المختصر بإزالة اللاحقة `.CA` من كود التداول.

أُنشئ جدول `egx_companies` في PostgreSQL عبر migration مستقلة، ثم نُفّذ استيراد idempotent باستخدام `upsert`. النتيجة الفعلية في Neon هي 145 شركة EGX، مع uniqueness على `sourceCode` و`symbol` وفهرس على `market, symbol`. أصبح محول دليل الشركات يقرأ الكتالوج المحلي أولاً، ويستخدم مزود البيانات الخارجي كـ fallback فقط عند خلو الجدول.

| الحقل | النتيجة |
|---|---:|
| عدد الصفوف المستوردة | 145 |
| عدد أكواد المصدر الفريدة | 145 |
| عدد الرموز المختصرة الفريدة | 145 |
| السوق | EGX |
| SHA-256 للملف الأصلي | `68e2a18c8af666dabed6bac877d520705aa6ee2eefafa19becc93e242e735bb7` |

تم التحقق من `npm run typecheck`، ونجاح 26 اختبار خادم، ونجاح `npm run build`، كما طُبقت migration بنجاح ونفذ أمر الاستيراد بنتيجة `imported: 145` و`totalEgxCompanies: 145`.
