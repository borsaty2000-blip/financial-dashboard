import { useLocalSearchParams } from 'expo-router'
import { Share, StyleSheet, Text, TouchableOpacity, View } from 'react-native'
import { ScreenContainer } from '../../src/components/ScreenContainer'
export default function StockDetailScreen() { const { symbol } = useLocalSearchParams<{ symbol: string }>(); const share = () => void Share.share({ message: `تحليل ${symbol} عبر بورصتي: borsaty://stock/${symbol}` }); return <ScreenContainer><Text style={styles.back}>بورصتي / تفاصيل السهم</Text><Text style={styles.symbol}>{symbol}</Text><View style={styles.chart}><Text style={styles.muted}>الرسم الشمعي والتحليلات تظهر عند توفر البيانات.</Text></View><View style={styles.grid}>{['Elliott Wave', 'Gann', 'Consensus', 'RSI / MACD'].map((item) => <View style={styles.card} key={item}><Text style={styles.heading}>{item}</Text><Text style={styles.muted}>—</Text></View>)}</View><TouchableOpacity style={styles.button} onPress={share}><Text style={styles.buttonText}>مشاركة التحليل</Text></TouchableOpacity></ScreenContainer> }
const styles = StyleSheet.create({ back: { color: '#64748b', textAlign: 'right', marginTop: 18 }, symbol: { fontSize: 32, fontWeight: '800', textAlign: 'right', marginVertical: 10 }, chart: { minHeight: 220, borderRadius: 12, backgroundColor: '#fff', alignItems: 'center', justifyContent: 'center' }, grid: { flexDirection: 'row-reverse', flexWrap: 'wrap', gap: 8, marginVertical: 12 }, card: { width: '48%', backgroundColor: '#fff', borderRadius: 10, padding: 12, gap: 6 }, heading: { fontWeight: '800', textAlign: 'right' }, muted: { color: '#64748b', textAlign: 'right' }, button: { backgroundColor: '#0878bd', padding: 14, borderRadius: 10, alignItems: 'center' }, buttonText: { color: '#fff', fontWeight: '800' } })
